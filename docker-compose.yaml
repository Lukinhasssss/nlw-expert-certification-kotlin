version: '3.9'

services:
  postgres_nlw_expert:
    container_name: postgres_nlw_expert
    image: postgres
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=certification